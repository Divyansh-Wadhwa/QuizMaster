[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/api/auth/admin/users/count"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "8080"
JAVA_OPTS = "-Xmx512m -Xms256m"
